1. Define the canonical supplier record shape and required fields.
2. Define the role, ownership, and masking rules for list, detail, update, import, and export.
3. Define the pipe-delimited import preview contract and auto-rating behavior.
4. Define the CSV export contract that reuses the same visibility projection as list and detail.
5. Promote the finalized change into `openspec/specs/workspace-suppliers/spec.md`.
