# 光速上升设计风格规范

本文件是项目根目录下的统一设计风格说明，后续页面、组件和改版都应以此为准。

## 1. 设计方向

- 整体气质：克制、沉稳、偏品牌展陈感，不做通用后台风，也不做常见电商模板风。
- 视觉基调：深色背景为主，暖金色作为强调色，形成“深色画布 + 金属感高光”的品牌印象。
- 页面层次：靠留白、材质分层、弱边框和阴影建立层次，不靠密集分割线堆砌界面。
- 信息表达：标题要有识别度，正文要稳定易读，辅助信息要明显弱于主信息。

## 2. 唯一设计值来源

所有视觉 tokens 统一来自 [src/theme/tokens.js](/Users/23271/Desktop/光速上升/front/src/theme/tokens.js)。

允许直接引用的设计值范围：

- 颜色
- 渐变
- 圆角
- 阴影
- 间距
- 字体与字号
- 动效时长与悬浮位移

禁止事项：

- 在页面或组件里随意新增十六进制颜色
- 在局部样式里重新定义一套圆角体系
- 为单个页面临时写一套阴影和留白规则
- 为了“看起来差不多”绕开 tokens

如果现有 tokens 不够用，先补充到 `src/theme/tokens.js`，再在组件中使用。

## 3. 当前主题基线

### 颜色

- 主背景：接近 `#111111` 的深色画布
- 次级背景：略抬升的深灰表面，用于卡片和面板
- 主文字：暖白色，不使用纯白
- 次级文字：降低对比度的米灰色
- 强调色：暖金色到棕金色渐变，不回退到紫色系或蓝紫科技风
- 边框：极弱透明白或弱金色描边，只作为层次辅助

### 圆角

- Hero 容器：大圆角
- 面板：中大圆角
- 卡片：中圆角
- 图片容器：略小于卡片
- 按钮与筛选控件：偏胶囊形

页面中同类组件必须共用同层级圆角，不允许一页内出现杂乱无章的多个近似值。

### 阴影

- 浮层、主卡片、重点按钮允许使用较明显阴影
- 普通信息卡片使用轻阴影或仅靠背景抬升
- 小组件不要叠加重阴影，避免界面发脏

### 间距

- 页面左右边距、区块上下间距、卡片内边距必须来自统一 spacing tokens
- 大区块保持充足留白，强调品牌站而不是表格式堆叠
- 同类卡片的内边距保持一致，不要局部跳值

### 字体

- 中文默认采用 `Noto Sans SC`、`PingFang SC`、`Microsoft YaHei` 这一类无衬线字体栈
- 标题通过字号和字重建立层级，不依赖花哨字体
- 正文优先保证可读性，不使用过细字重

### 动效

- 过渡应短、稳、克制
- 常用动效为淡入、轻微上浮、轻微缩放反馈
- 不叠加炫技型动画，不使用强烈跳跃感 motion

## 4. 页面文案规则

- 全站通用 UI 文案统一使用中文
- 仅以下内容允许保留英文：
  - `GLINT RISE`
  - 产品型号名或系列专名
  - 案例专有英文名
  - 路由 slug
  - 数据 `id`
- `alt`、可访问性标签、分享标题、空状态提示也属于文案范围，必须遵守同样规则

## 5. 组件落地规则

- 先引用 tokens，再写页面结构和局部样式
- 优先复用共享组件，不重复造导航、页脚、卡片、筛选条、标题模块
- 同类组件在不同页面中应保持统一视觉语言
- 页面新增视觉样式时，先判断能否落到共享 tokens 或共享组件

## 6. 新页面开发流程

1. 先确认路由归属和页面角色
2. 先检查是否已有可复用的内容数据与共享组件
3. 优先使用 [src/theme/tokens.js](/Users/23271/Desktop/光速上升/front/src/theme/tokens.js) 中已有 tokens
4. 再补充局部布局和交互
5. 最后做中文化检查和页面回归验证

## 7. 反模式

以下做法视为违背当前设计规范：

- 页面里直接硬编码新颜色而不进入 tokens
- 同类按钮、卡片、标题在不同页面完全不是一套语言
- 通用 UI 文案继续保留英文
- 同一页面里随意混用多种圆角、阴影、渐变
- 为单个活动页私造一套不复用的设计规则

## 8. 参考文件

- [src/theme/tokens.js](/Users/23271/Desktop/光速上升/front/src/theme/tokens.js)
- [docs/design-style-guide.md](/Users/23271/Desktop/光速上升/front/docs/design-style-guide.md)
- [src/data/siteContent.js](/Users/23271/Desktop/光速上升/front/src/data/siteContent.js)
- [src/App.jsx](/Users/23271/Desktop/光速上升/front/src/App.jsx)

后续如果设计基线有变化，应优先更新本文件和 `src/theme/tokens.js`，再进行页面级改动。
