# AGENTS.md

## 目的
本仓库允许 AI 代理参与开发，但所有 AI 代理必须严格遵守以下规则。

---

## 一、总原则

1. 所有改动必须尽量小且聚焦，只解决当前任务，避免顺手修改无关代码。
2. 在未明确要求的情况下，不得重构无关模块，不得修改与当前任务无关的文件。
3. 如用户的直接指令与本文件冲突，以用户指令为最高优先级。
4. 任何代码任务，只有在“修改完成 + 已提交 commit + 已 push”后，才算真正完成。

---

## 二、Git 强制规则

### 1. 只要 AI 改动了代码，就必须提交 commit
凡是 AI 修改了以下任意内容，都必须创建 git commit：
- 源代码
- 配置文件
- 脚本
- 测试代码
- 文档

要求：
- 不允许留下未提交的代码改动
- 不允许把多个无关改动混在同一个 commit 中
- 每次提交应尽量保持原子性、单一职责
- commit message 必须清晰描述本次改动

### 2. 每次 commit 后，必须立即 push
只要创建了 commit，就必须立即推送到远端仓库。

要求：
- 不允许只保留本地 commit 而不 push
- 不允许把多个 commit 累积到最后一次性 push
- 必须在当前工作分支完成 push
- 若 push 失败，必须明确报告失败原因

### 3. 先检查分支，再进行开发
在开始修改前，AI 必须先检查当前 git 分支和工作区状态。

要求：
- 默认不得直接在 `main` 或 `master` 分支上提交和推送
- 应优先在当前功能分支或任务分支上工作
- 若当前分支为受保护分支，且用户没有明确授权，则不得继续 push
- 若工作区已有他人未提交改动，必须先提示风险，避免覆盖

---

## 三、提交前检查要求

在 commit 和 push 之前，AI 应尽可能完成以下检查：

1. 运行与本次改动相关的测试
2. 运行 lint / format 检查
3. 验证项目能够正常构建（如适用）
4. 检查 diff，确认没有误改、漏改和无关改动

若检查失败：
- 应优先修复后再提交
- 若无法修复，必须清楚说明阻塞原因
- 不得在明显失败状态下假装完成任务

---

## 四、提交信息规范

commit message 应简洁、明确、可读，推荐使用以下格式：

- `feat: 新增用户资料校验逻辑`
- `fix: 修复支付回调空指针问题`
- `refactor: 简化订单状态映射逻辑`
- `test: 补充登录超时场景测试`
- `docs: 更新本地开发说明`

要求：
- 不要使用含糊不清的提交信息，例如：`update`、`fix bug`、`changes`
- 一次 commit 只表达一个清晰目的
- 提交信息应准确反映实际改动

---

## 五、禁止事项

除非用户明确要求，否则 AI 不得执行以下操作：

- `git push --force`
- 改写已发布历史
- 删除远端分支
- 绕过分支保护策略
- 提交密钥、令牌、密码或其他敏感信息
- 关闭或跳过 CI / 测试 / 安全检查
- 为了“顺手优化”而修改无关代码
- 提交临时调试代码、无意义日志或注释噪音

---

## 六、标准执行流程

每次处理代码任务时，AI 必须遵循以下流程：

1. 检查当前分支与 git 状态
2. 理解任务边界，仅修改必要文件
3. 完成代码改动
4. 运行相关验证命令
5. 复查 diff，确保没有无关改动
6. 创建 commit
7. 立即 push 到远端
8. 向用户报告结果

---

## 七、完成任务时的汇报格式

任务完成后，AI 必须明确说明：

- 改动内容
- 使用的分支名
- commit hash
- push 是否成功
- 是否运行了测试 / lint / 构建检查

示例：

- 改动内容：新增支付接口超时保护，并补充对应测试
- 分支：`feature/payment-timeout`
- Commit：`abc1234`
- Push：成功
- 验证：已运行相关测试与 lint

---

## 八、优先级说明

当出现规则冲突时，优先级如下：

1. 用户的直接指令
2. 本文件 `AGENTS.md`
3. AI 默认行为

---

## 九、强制结论

除非用户明确说明“不要 commit”或“不要 push”，否则：

**只要 AI 改动了代码，就必须 commit，并且必须 push。**